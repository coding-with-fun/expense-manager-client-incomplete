/**
 * @harsh-coderc
 * @description - Sign In and Sign Up forms
 * @param
 *      - routeType
 *          - 0 - Sign In
 *          - 1 - Sign Up
 */

import React from "react";

const AuthForm = ({ routeType }) => {
    return <div>Auth Form {routeType}</div>;
};

export default AuthForm;
